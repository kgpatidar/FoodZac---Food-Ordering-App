<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.AndroKG.foodzac">

    <application
        android:allowBackup="true"
        android:icon="@drawable/foodzacicon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/foodzacicon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".PastOrderClass"></activity>
        <activity android:name=".FInalOrderPage" />
        <activity android:name=".RestuarantPage" />
        <activity android:name=".ProfileDetail" />
        <activity android:name=".HomePage" />
        <activity android:name=".LoginSignup" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>